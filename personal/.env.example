# Template for personal/.env — copy to personal/.env and fill in real values.
# personal/.env is gitignored. NEVER commit real secrets to this repo.
# Real secrets live at ~/.config/dotfiles-secrets/.env (chmod 600).
export AGENTIC_API_KEY=
export AGENTIC_APP_SECRET_KEY=
